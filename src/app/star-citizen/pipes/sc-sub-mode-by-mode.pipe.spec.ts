import { ScSubModeByModePipe } from './sc-sub-mode-by-mode.pipe';

describe('ScSubModeByModePipe', () => {
  it('create an instance', () => {
    const pipe = new ScSubModeByModePipe();
    expect(pipe).toBeTruthy();
  });
});
